<% if current_user  %>
  <div class="container">
        <div class="jumbotron">
          <p class="lead">
          <h2>Favoritos</h2>
          </p>
        </div>
<table style="width:100%">
  <tr>
    <th>Academia</th>
    <th></th>
  </tr>
  <% current_user.all_following.each do |gym| %>
    <tr>
      <td><%= gym.name%></td>
      <td><%= link_to 'Detalhes', gym %></td>
    </tr>
  <% end %>
</table>

<% else %>
  <body>
    <div class="container">
      <div class="jumbotron">
        <h3 class="display-4"><%= link_to "Login necessário", "/auth/google" %></h3>
        <p class="lead"></p>
      </div>
<% end %>